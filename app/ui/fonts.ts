import { Inter, Itim, Lusitana } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] })
export const itim = Itim({ weight: '400', subsets: ['vietnamese'] })
export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
})
